#iuclude <iostream>

template<class R> class Square{
public:
  T data;
  Square(T data){data = _data * _data;}
  ~Square(){};
};
main(){
  Square<int> a(10);
  Square(double)> b(10.1);
    
  std::cerr << a.data << std::endl;
  std::cerr << b.data << std::endl;
 

}
